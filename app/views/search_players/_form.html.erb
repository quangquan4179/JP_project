<%= form_with(model: search_player, local: true) do |form| %>
  <% if search_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search_player.errors.count, "error") %> prohibited this search_player from being saved:</h2>

      <ul>
        <% search_player.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :player_name %>
    <%= form.text_field :player_name %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country %>
  </div>

  <div class="field">
    <%= form.label :club %>
    <%= form.text_field :club %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
